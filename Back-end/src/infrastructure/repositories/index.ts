/**
 * Infrastructure Repositories Index
 * Exports all MongoDB repository implementations
 */

export { BaseMongoRepository } from './base/BaseMongoRepository';

// For concrete implementations, import from implementations.ts:
// import { MongoStudentRepository } from './implementations';
